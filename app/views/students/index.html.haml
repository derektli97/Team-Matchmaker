%h1 Listing students

%table
  %thead
    %tr
      %th First name
      %th Last name
      %th Email
      %th Gender
      %th Ethnicity
      %th Topics
      %th Hardware
      %th Electives
      %th Preferences
      %th Section
      %th Project
      %th
      %th
      %th

  %tbody
    - @students.each do |student|
      %tr
        %td= student.first_name
        %td= student.last_name
        %td= student.email
        %td= student.gender
        %td= student.ethnicity
        %td= student.topics
        %td= student.hardware
        %td= student.electives
        %td= student.preferences
        %td= student.section_id
        %td= student.project_id
        %td= link_to 'Show', student
        %td= link_to 'Edit', edit_student_path(student)
        %td= link_to 'Destroy', student, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Student', new_student_path
